version: 0.2

phases:
    install:
      commands:
        - echo "nothing to install"
      build:
         commands:
            - echo "Building static website..."
      artifacts:
            files:
                - '**/*'